//! rfo — GitHub-first repo orchestration CLI.
//!
//! Binary entry point. CLI parsing lives here; all logic is in library crates.

fn main() {
    println!("rfo: repo forge orchestrator — not yet wired");
}
